// src/pages/client/SupportPage.tsx
import { Box, Typography } from "@mui/material";

const SupportPage = () => {
  return (
    <Box>
      <Typography variant="h4">Support</Typography>
      <Typography>Contact support and help resources</Typography>
    </Box>
  );
};

export default SupportPage;